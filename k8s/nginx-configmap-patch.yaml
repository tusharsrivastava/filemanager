# Patches the nginx ingress controller's global ConfigMap to remove the
# default body-size limit. Apply this if uploads still return 413 despite
# the per-ingress proxy-body-size: "0" annotation.
#
# The controller ConfigMap is usually in the ingress-nginx namespace.
# Confirm with: kubectl get configmap -n ingress-nginx
#
# Apply:
#   kubectl apply -f k8s/nginx-configmap-patch.yaml
#
# Or patch in-place without modifying a file:
#   kubectl patch configmap ingress-nginx-controller \
#     -n ingress-nginx \
#     --type merge \
#     -p '{"data":{"proxy-body-size":"0","proxy-read-timeout":"600","proxy-send-timeout":"600"}}'
apiVersion: v1
kind: ConfigMap
metadata:
  name: ingress-nginx-controller   # ← default name; check yours with kubectl get configmap -n ingress-nginx
  namespace: ingress-nginx          # ← default namespace for the nginx ingress controller
data:
  # "0" disables client_max_body_size — required for large file uploads
  proxy-body-size: "0"
  proxy-read-timeout: "600"
  proxy-send-timeout: "600"
  proxy-connect-timeout: "600"
