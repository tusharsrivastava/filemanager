# Path to the directory the file manager will serve.
# In Kubernetes, set this to your PVC mount path (e.g. /data or /media).
# In local dev, leave unset — defaults to ./data in the project root.
MOUNT_PATH=/data
